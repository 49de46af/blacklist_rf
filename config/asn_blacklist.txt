# ASNs to force-include in the blacklist (regardless of pattern matching)
# Format: one ASN per line, comments with #
#
# Main ASN list moved to rkn_collaborants.txt
# Use this file for one-off force-includes into the base blacklist
